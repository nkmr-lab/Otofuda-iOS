# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "cocoapods"
gem "fastlane"

# プロジェクトメンバーでバージョンを固定したい場合は以下のように書く
# gem "cocoapods", "1.2.1"